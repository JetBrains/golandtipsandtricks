package main

import "fmt"

func main() {
	// Completion with Tab key instead of Enter overwrites the identifier

	// E.g. Here invoke Completion then type Prl and press Tab instead of
	//  Enter to complete Println instead of Printf

	fmt.Printf("各位 Gopher 晚上好")
}
